Arrays - ek see jyada memebers rakhne ki jagah
1. Ways to create an array 

let arr = [112,208,348,462,5012]
let arr2 = new Array();

2. how to acess elements in array

let arr = [112,208,348,462,5012]
console.log(arr[0])

3. function on array -> push , pop, shift, unshift, indexOf, array destructuring, filter, some, map, reduce, spread operator,splice, slice, reverse, sort, join, tostring

4. iretation over arrays using - for loop, forEach


objects in JS----
1. Understanding What are Objects in JavaScript - [`key-value pair` ]

2. Creating Objects, Accessing Properties, Deleting Property and Nested Objects.

3. Recognise How Objects Are Stored, Traverse Keys of an Object, Array as Object.

4. Timing Events - `setTimeout()` , `setInterval()` , `clearTimeout()` , `clearInterval()`

5. Operation in Objects - [`freeze` , `seal` , `destructuring` , `object methods` , `this keyword`]